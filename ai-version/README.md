# AI Version

This folder contains the isolated AI-generated implementation for Stage 7.

## Run

```bash
npm install
npm start
```

The API runs at `http://localhost:3001` and Swagger UI is available at `http://localhost:3001/docs/`.

This version is intentionally kept separate from the hand-built implementation in the parent `src/` directory.
