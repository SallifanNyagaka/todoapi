# AI Version 2

This is the improved rematch generated from `PROMPT.md` after reviewing the first AI version.

Run it with:

```bash
npm install
npm start
```

It runs at `http://localhost:3002` and documents the API at `http://localhost:3002/docs/`.
